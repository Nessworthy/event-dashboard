export type ObjectEvent = {
    event_id: string
    event_name: string,
    event_detail: string,
    event_timestamp: number,
    event_time: string,
    event_severity: string,
    object_type: string,
    object_name: string
}
